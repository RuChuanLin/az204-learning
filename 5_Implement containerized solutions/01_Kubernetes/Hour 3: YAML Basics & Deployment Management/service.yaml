apiVersion: v1
kind: Service
metadata:
  name: hello-node-service       # Clear service name
spec:
  type: NodePort
  selector:
    app: hello-node-app          # MUST match the Deployment pod label
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080            # Optional: assign fixed NodePort
